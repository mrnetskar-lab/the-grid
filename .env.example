# The Grid — environment variables
# Copy to .env and fill in at least one AI provider key

# AI Provider (first one found wins)
OPENROUTER_API_KEY=
GROQ_API_KEY=
TOGETHER_API_KEY=
OPENAI_API_KEY=

# Optional: override model globally
# AI_MODEL=

# Optional: per-character model overrides (OpenRouter model IDs)
# NINA_MODEL=
# HAZEL_MODEL=
# IRIS_MODEL=
# VALE_MODEL=

PORT=3001
