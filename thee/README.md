Grid v2 prototype

Open `index.html` in this folder to view the Velora mockup preview.

To preview locally:

```bash
# from repo root
# open the file in your browser (Windows)
start thee\index.html
# or (mac/linux)
open thee/index.html
```

If you want me to fully wire this into a served dev server or copy more assets, tell me which pieces to bring over from the main repo.